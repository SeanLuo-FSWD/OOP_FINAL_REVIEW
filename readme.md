Quickstart: 1. Install all dependencies:
npm i

    2. Install parser:
        npm i -g parcel-bundler

    3. Run frontend build
        npm run frontendBuild

    4. Run backend build and start server
        npm run backendWatch
